apiVersion: v1
kind: Service
metadata:
  name: payment-service

spec:
  selector:
    app: payment-service

  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000

  type: {{ .Values.service.type }}
  