apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: payment-service

spec:
  ingressClassName: nginx

  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: payment-service
                port:
                  number: 5000