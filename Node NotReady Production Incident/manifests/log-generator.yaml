apiVersion: v1
kind: Pod
metadata:
  name: log-generator
spec:
  containers:
  - name: logger
    image: busybox
    command:
      - /bin/sh
      - -c
      - |
        while true
        do
          echo "$(date) ERROR Application failure simulation" >> /tmp/app.log
          sleep 0.01
        done